# ─────────────────────────────────────────────────────────────────────────────
# requirements_gpu.txt
# GPU training extras (beyond requirements.txt)
# Install with: pip install -r requirements_gpu.txt
#
# NOTE: Install a CUDA-enabled PyTorch build from https://pytorch.org/get-started/locally/
# that matches your driver. Do not pip-install a second torch build on top of a CUDA image.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core stack ────────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
pyarrow>=14.0.0          # Fast Parquet I/O for tick data

# ── High-performance data ─────────────────────────────────────────────────────
polars>=0.19.0            # 2-5× faster than Pandas for multi-million row datasets
numba>=0.58.0             # GPU-accelerated Triple Barrier calculation
aiohttp>=3.9.0            # Async HTTP for Dukascopy parallel downloader (128 concurrent)
yfinance>=0.2.40          # Yahoo fallback for cross-asset panel

# ── Reinforcement Learning ────────────────────────────────────────────────────
stable-baselines3>=2.2.0  # PPO / DQN — plugs directly into ForexScalingEnv
gymnasium>=0.29.0         # OpenAI Gym successor (required by SB3)
shimmy>=1.0.0             # Gym compatibility shim

# ── Experiment tracking ───────────────────────────────────────────────────────
wandb>=0.16.0             # Weights & Biases — loss curves, GPU utilisation, alerts
tensorboard>=2.15.0       # TensorBoard
tensorboardX>=2.6.0
prometheus_client>=0.20.0 # Metrics exporter for Prometheus/Grafana

# ── Hyperparameter optimisation ───────────────────────────────────────────────
optuna>=3.5.0             # Bayesian HPO — finds best lr, hidden_size, etc.
optuna-dashboard>=0.15.0  # Visual HPO dashboard

# ── Utilities ─────────────────────────────────────────────────────────────────
loguru>=0.7.0
tqdm>=4.66.0
rich>=13.0.0              # Pretty terminal output
pyyaml>=6.0
python-dotenv>=1.0.0
joblib>=1.3.0
h5py>=3.10.0              # HDF5 for large dataset storage
einops>=0.7.0             # Tensor rearrangement for Transformer models
timm>=0.9.0               # Pre-trained vision models (optional, for embeddings)

# ── Notebooks / monitoring ──────────────────────────────────────────────────
jupyterlab>=4.0.0         # JupyterLab

# ── Optional: News / Sentiment (Dual-Stream) ─────────────────────────────────
#Uncomment when enabling DUAL_STREAM in config/settings.py:
transformers>=4.36.0    # FinBERT / Mistral-7B for news sentiment
sentencepiece>=0.1.99   # Tokeniser required by FinBERT
accelerate>=0.25.0      # Efficient multi-GPU transformer inference
